# Folio Landing Page

Source for the marketing site. `npm run build:landing` copies these files to `public/`.

When deploying to Vercel, either commit this folder or rely on the committed `public/` files.
